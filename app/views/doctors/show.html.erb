<h3> Your Appointments </h3> 

<h3>Appointments:</h3>
<ul>
    <% @doctor.appointments.each do |appt| %>
        <li><%= link_to appt.appointment_info, appt %> <%= link_to appt.patient_name, patient_path(appt.patient) %>, </li>
    <% end %>
</ul>

    