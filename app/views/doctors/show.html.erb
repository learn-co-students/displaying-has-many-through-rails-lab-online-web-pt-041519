<h2><%= @doctor.name %>'s Appointment List</h2>

<ul><% @doctor.appointments.each do |a| %>
    <li><%= link_to a.patient.name, patient_path(a.patient) %> - 
        <%= a.appointment_datetime.strftime("%B %e, %Y at %R") %>
</li>
<% end %></ul>