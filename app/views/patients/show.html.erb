<h2><%= @patient.name %>'s Appointment List</h2>

<ul><% @patient.appointments.each do |appt| %>
    <li><%= link_to appt.doctor.name, doctor_path(appt.doctor) %> - 
        <%= appt.appointment_datetime.strftime("%B %e, %Y at %R") %>
</li>
<% end %></ul>