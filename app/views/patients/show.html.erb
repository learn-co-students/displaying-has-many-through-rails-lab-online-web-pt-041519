<h2><%= @patient.name %>'s Upcoming Appointments</h2>

<ul><% @patient.appointments.each do |a| %>
    <li><%= a.appointment_datetime.strftime("%B %e, %Y at %R") %> with Doctor 
    <%= link_to a.doctor.name, doctor_path(a.doctor) %>
        
</li>
<% end %></ul>